<UserControl
    x:Class="OneBreak.Controls.NewsItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneBreak.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Image Stretch="Uniform" Source="{Binding CoverArtImageUrl}"/>
        <Grid>
            <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
                <StackPanel.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#7F707070" Offset="0"/>
                        <GradientStop Color="#FF323232" Offset="1"/>
                    </LinearGradientBrush>
                </StackPanel.Background>
                <TextBlock Text="{Binding Title}"/>
                <TextBlock Text="{Binding Description}"/>
                <TextBlock Text="Provider" TextAlignment="Right"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
